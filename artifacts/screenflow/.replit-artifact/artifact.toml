kind = "web"
previewPath = "/"
title = "ScreenFlow — Screen Recording SaaS"
version = "1.0.0"
id = "artifacts/screenflow"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20931

[services.development]
run = "pnpm --filter @workspace/screenflow run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/screenflow", "run", "build" ]
publicDir = "artifacts/screenflow/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20931"
BASE_PATH = "/"
